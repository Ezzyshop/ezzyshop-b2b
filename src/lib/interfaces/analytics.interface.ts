export interface ITotalAnalytics {
  total_revenue: {
    current_month: number;
    last_month: number;
  };
  total_customers: {
    current_month: number;
    last_month: number;
  };
  total_sales: {
    current_month: number;
    last_month: number;
  };
}
